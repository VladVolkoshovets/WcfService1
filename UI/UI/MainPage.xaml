<Page x:Class="UI.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:UI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainPage">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="5*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <StackPanel Name="ButtonsPanel" Background="{DynamicResource icons}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition></ColumnDefinition>
                    <ColumnDefinition Width="2*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Button>1</Button>
                <TextBox  Text="Search" Grid.Column="1" TextAlignment="Center" HorizontalAlignment="Center"></TextBox>
            </Grid>
            <Button x:Name="SupperButton" Background="{DynamicResource icons}" BorderBrush="{DynamicResource divider}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Height="83" Padding="4">
                <Grid x:Name="MyGrid">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="2*"></ColumnDefinition>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Background="Transparent" Name="RoomName" Grid.Column="1" Foreground="{StaticResource primary_text}" FontSize="14" Margin="3">Vlad</TextBlock>
                    <TextBlock Background="Transparent" Name="LastMessage" Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="2" Foreground="{StaticResource primary_text}" FontFamily="Verdana" FontSize="10" IsHyphenationEnabled="True">Some mesage fksdj fjsd jf</TextBlock>
                    <Button Grid.RowSpan="2" Height="50" Width="50" Style="{DynamicResource MaterialDesignFloatingActionAccentButton}"  FontSize="11" Margin="0" IsHitTestVisible="False">
                        <Image Source="Images/icon_user.png" Height="35" />
                    </Button>
                </Grid>

            </Button>
            <Button Style="{DynamicResource MaterialDesignRaisedButton}" Background="White" BorderBrush="White">1</Button>

        </StackPanel>
        <Grid Grid.Column="1">
            <Frame x:Name="ChatFrame"></Frame>
        </Grid>
    </Grid>
</Page>
